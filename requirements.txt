# Python 3.11.x - versions per thesis §3.9 (Table: Software Specification).
#
# These are the compatible RANGES the study was designed against. For an
# exact byte-for-byte reproduction of the environment that produced the
# reported results, install from requirements-lock.txt instead:
#     pip install -r requirements-lock.txt
# (generated by pip freeze from the venv used for the experiments;
#  Python 3.11.9, torch 2.2.2+cpu).
torch==2.2.*
transformers==4.41.*
sentence-transformers==2.7.*
scikit-learn==1.4.*
xgboost==2.0.*
pandas==2.2.*
numpy==1.26.*
pyarrow>=15.0
matplotlib>=3.8
seaborn>=0.13
umap-learn>=0.5.5
joblib>=1.3
pytest>=8.0
kaggle>=1.6
